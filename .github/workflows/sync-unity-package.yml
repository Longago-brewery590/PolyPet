name: Sync Unity Package to Sample

on:
  push:
    branches: [main]
    paths: [Unity/**]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Copy package to Unity sample
        run: |
          rm -rf Samples/PolyPetDemoUnity/Packages/com.shilo.polypet
          mkdir -p Samples/PolyPetDemoUnity/Packages
          cp -r Unity Samples/PolyPetDemoUnity/Packages/com.shilo.polypet

      - name: Commit if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -f Samples/PolyPetDemoUnity/Packages/com.shilo.polypet/
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "chore: sync Unity package to sample project"
          git pull --rebase origin main
          git push origin HEAD:main
